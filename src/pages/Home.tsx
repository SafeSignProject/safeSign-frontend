import { Button, Input } from '@/components/common';
import { Badge } from 'lucide-react';

export const Home = () => {
  return (
    <div>
      <Button type='button' label='버튼' />
      <Badge />
      <Input />
    </div>
  );
};
